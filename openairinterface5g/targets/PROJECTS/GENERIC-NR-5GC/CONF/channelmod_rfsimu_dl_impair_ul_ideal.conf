# DL만 왜곡/잡음 (UE RX): rfsimu_channel_enB0
# UL은 이상에 가깝게 (gNB RX): rfsimu_channel_ue0 — 단위 채널(AWGN) + 극소 잡음
#
# UE 쪽 수신(DL): enB0 모델 적용
# gNB 쪽 수신(UL): ue0 모델 적용 (simulator.c 역할별 legacy 이름)

channelmod = {
  max_chan = 10;
  modellist = "modellist_rfsimu_1";
  modellist_rfsimu_1 = (
    {
      model_name      = "rfsimu_channel_enB0"
      type            = "AWGN";
      ploss_dB        = 0;
      noise_power_dB  = -70;
      forgetfact      = 0.0;
      offset          = 0;
    },
    {
      model_name      = "rfsimu_channel_ue0"
      type            = "AWGN";
      ploss_dB        = 0;
      noise_power_dB  = -120;
      forgetfact      = 0.0;
      offset          = 0;
    }
  );
};
